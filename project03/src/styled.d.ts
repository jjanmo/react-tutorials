import 'styled-components';

declare module 'styled-components' {
  export interface DefaultTheme {
    bgColor: string;
    textBlack: string;
    textBlue: string;
    themeColor: string;
    riseColor: string;
    fallColor: string;
  }
}
